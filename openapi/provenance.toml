# Machine-checked vendoring record — the single source of truth for what was vendored.
# `cargo run -p xtask -- vendor-spec --check` and `tests/openapi_parity.rs` assert
# `vendored_sha256` and `operations` against the committed files, so this file cannot go
# stale silently. PROVENANCE.md narrates it and deliberately keeps no second copy.
#
# `source_sha256` records the upstream bytes the vendored file came from. It is provenance
# only: nothing offline can re-verify it, and re-fetching may legitimately change it.

[exa]
source_url = "https://exa.ai/docs/exa-spec.json"
vendored_path = "openapi/exa-openapi.json"
source_sha256 = "5540b4a81b5b7699128209c3070aa96810998b964e61b84ecff53fab53e6f702"
vendored_sha256 = "59518d9f6af69b004eb05d3f12d969bc87cc74bf9acd011516dd14293f52e43c"
operations = 64
verified = "2026-09-21"

[admin]
source_url = "https://exa.ai/docs/team-management-spec.yaml"
vendored_path = "openapi/team-management.json"
source_sha256 = "3cb487d56b67a3f0ae45fe0750341a9ceedfbd6084b36bf1b14b0802bb785c8d"
vendored_sha256 = "35b19be1f94438af071f394dddec15c96cf9641fe0346b04c01755558ff66170"
operations = 6
verified = "2026-09-21"
