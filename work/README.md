# work/

Development-history notes: research lanes, spec snapshots, and run ledgers kept from building this CLI. Not user-facing documentation — see the top-level `README.md` and `AGENTS.md` for that, and `docs/v2/` for the canonical design record.
